<h1><%= @place["name"] %></h1>

<% if session["user_id"] != nil %>
  <a href="/entries/new?place_id=<%= @place["id"] %>" class="btn btn-primary mb-3">New Post</a>
<% end %>

<div class="row">
  <% for entry in @entries %>
    <div class="col-md-4 mb-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title mb-1"><%= entry["title"] %></h5>
          <p class="card-subtitle text-muted mb-2"><%= entry["occurred_on"] %></p>
          <% if entry.uploaded_image.attached? %>
            <img src="<%= url_for(entry.uploaded_image) %>" class="img-fluid mb-3" style="height: 200px; object-fit: cover; width: 100%;">
          <% end %>
          <p class="card-text"><%= entry["description"] %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>
<!-- I used chat gpt to come up with the format above; i made the image sizes fixed, make it so there would be 3 posts in a row or less when minimzied, and changed how the text looked for title vs. date -->

<p>
  <a href="/places" class="btn btn-primary mb-3">Back to All Places</a>
</p>